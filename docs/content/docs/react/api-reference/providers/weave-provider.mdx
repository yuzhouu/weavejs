---
title: WeaveProvider
description: API reference for WeaveProvider component
---

import { TypeTable } from "fumadocs-ui/components/type-table";

## Overview

The WeaveProvider is a core component from the React Helper library that initializes and
provides Weave.js context to your React application. It sets up the needed elements for real-time
collaboration and canvas rendering:

- Store
- Nodes
- Plugins
- Actions
- Fonts

By wrapping your app (or part of it) with WeaveProvider, you can make use of the [useWeave](/docs/react/api-reference/hooks/use-weave)
hook to access the API of the Weave instance.

<Callout title="Nodes, Actions and Plugins by default?" type="warn">
  By default this helper doesn't include any nodes, actions or plugins. You must
  add your own.
</Callout>

## Import

```shell
import { WeaveProvider } from "@inditextech/weave-react";
```

## types

```ts
export type WeavePerformanceConfig = {
  upscale?: Partial<WeaveUpscaleConfig>;
};

export type WeaveUpscaleConfig = {
  enabled?: boolean;
  multiplier?: number;
  baseWidth?: number;
  baseHeight?: number;
};
```

## Props

<TypeTable
  type={{
    getContainer: {
      required: true,
      description:
        "A function that returns an HTMLDivElement where Weave.js will setup",
      type: "() => HTMLDivElement",
    },
    store: {
      required: true,
      description:
        "Weave.js Store to use to handle the real-time communication",
      type: "WeaveStore",
    },
    renderer: {
      required: true,
      description:
        "Weave.js Renderer to use to handle the real-time reconciliation of the shared state",
      type: "WeaveRenderer",
    },
    children: {
      required: true,
      description: "Child nodes to render",
      type: "ReactNode",
    },
    nodes: {
      description: "Array of nodes to register and use by the instance",
      type: "WeaveNode[]",
      default: "[]",
    },
    plugins: {
      description: "Array of plugins to register and use by the instance",
      type: "WeavePlugin[]",
      default: "[]",
    },
    actions: {
      description: "Array of actions to register and use by the instance",
      type: "WeaveAction[]",
      default: "[]",
    },
    customPlugins: {
      description:
        "Array of custom plugins to register and use by the instance",
      type: "WeavePlugin[]",
      default: "[]",
    },
    fonts: {
      description: "Array that define the fonts to use on the canvas",
      type: "WeaveFont[]",
      default: "[]",
    },
    logLevel: {
      description: "Global log level of Weave.js instance",
      type: "WeaveLogLevel",
      default: "info",
    },
    logModules: {
      description:
        "Define a custom log level Weave.js internal modules, nodes, actions and plugins. When defining a custom level, the format is 'moduleName:level'",
      type: "WeaveChildLoggerLevel[]",
      default: "[]",
    },
    performance: {
      description: "Options to fine-tune performance of Weave.js instance",
      type: "WeavePerformanceConfig",
    },
  }}
/>
